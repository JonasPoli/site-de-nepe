{% extends 'admin/nepe_base.html.twig' %}
{% import 'admin/study/_form.html.twig' as forms %}

{% block page_title %}Novo Material{% endblock %}

{% block body %}
<div class="ph">
    <h1>Novo Material</h1>
    <a href="{{ path('admin_study_index') }}" class="btn btn-g">← Voltar</a>
</div>
<div class="card">
    {{ forms.study_form(study, categories, path('admin_study_new')) }}
</div>
{% endblock %}
